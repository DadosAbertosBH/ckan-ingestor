[project]
name = "ckan-orchestrator"
version = "0.1.0"
description = "Lightweight CKAN data pipeline orchestrator"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "aiomysql>=0.2.0",
    "alembic>=1.14.0",
    "nats-py>=2.9.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "ckan-ingestor",
]

[project.scripts]
orchestrator-api = "ckan_orchestrator.main:run"
orchestrator-worker = "ckan_orchestrator.worker.consumer:run_standalone"
orchestrator-scheduler = "ckan_orchestrator.services.scheduler:run_standalone"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

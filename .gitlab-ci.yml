image: docker
services:
  - docker:dind

build:
  stage: build
  script:
    - cd ingestor
    - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER $CI_REGISTRY --password-stdin
    - docker build --push -t $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA .

apiVersion: v2
name: ingestor-orchestrator
description: Helm chart for the Ingestor Orchestrator — lightweight CKAN data pipeline orchestrator
type: application
version: 0.4.0
appVersion: "latest"
dependencies:
  - name: iggy
    version: 0.5.0
    condition: iggy.enabled

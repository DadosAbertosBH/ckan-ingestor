suite: Apache Iggy dependency
templates:
  - charts/iggy/templates/deployment.yaml
tests:
  - it: deploys Iggy using the official chart with existing storage and credentials
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: iggy
      - equal:
          path: spec.template.spec.containers[0].image
          value: apache/iggy:0.8.0
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: IGGY_ROOT_USERNAME
            valueFrom:
              secretKeyRef:
                name: ingestor-orchestrator-csi
                key: IGGY_USERNAME
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: IGGY_ROOT_PASSWORD
            valueFrom:
              secretKeyRef:
                name: ingestor-orchestrator-csi
                key: IGGY_PASSWORD
      - equal:
          path: spec.template.spec.volumes[0].persistentVolumeClaim.claimName
          value: iggy-data

